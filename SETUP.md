# Mehediadmin — "গাইড পেজ" এডিটর ইনস্টলেশন নোট

এটা Vangcur-এ আগে বসানো `guide_pages` টেবিলের অ্যাডমিন-সাইড এডিটর। এটা কাজ করার আগে
Vangcur ডেলিভারির `sql/001_create_guide_pages.sql` টেবিলটা তৈরি হয়ে থাকতে হবে (একই Supabase প্রজেক্ট)।

## যা যা নতুন এসেছে

- `types/guides.ts` → নতুন ফাইল (Vangcur-এর সাথে হুবহু মিলিয়ে রাখা, ভবিষ্যতে দুই জায়গায় একসাথে আপডেট করবেন)
- `app/actions/guidePages.ts` → নতুন ফাইল — list/create/update/publish/delete/duplicate সার্ভার অ্যাকশন
- `components/guides/GuideFieldPrimitives.tsx` → নতুন ফাইল — ছোট রিইউজেবল ফর্ম-ফিল্ড
- `components/guides/GuideBlockEditors.tsx` → নতুন ফাইল — প্রতিটা ব্লক-টাইপের এডিটর ফর্ম
- `components/guides/GuideEditorModal.tsx` → নতুন ফাইল — পুরো পেজের এডিটর (মেটা + ব্লক লিস্ট)
- `components/guides/GuidePagesListModal.tsx` → নতুন ফাইল — "এই প্রোডাক্ট/ক্যাটাগরির সব পেজ" লিস্ট + নতুন পেজ তৈরির ফর্ম
- `components/products/ProductsTable.tsx` → **এক্সিস্টিং ফাইল রিপ্লেস** — অ্যাকশন কলামে 📄 বাটন যোগ, নামের নিচে `#id` দেখানো (productRecommendation ব্লকে বসানোর জন্য)
- `components/design/CategoriesPageClient.tsx` → **এক্সিস্টিং ফাইল রিপ্লেস** — প্রতিটা ক্যাটাগরি রো-তে "📄 গাইড পেজ" বাটন যোগ

## কীভাবে ব্যবহার করবেন

1. **প্রোডাক্ট লিস্ট পেজে** প্রতিটা প্রোডাক্টের অ্যাকশন কলামে 📄 বাটন — এখানে ক্লিক করলে সেই প্রোডাক্টের Installation Guide / App-Remote Guide পেজগুলো দেখা যাবে, "+" দিয়ে নতুন বানানো যাবে।
2. **ক্যাটাগরি ম্যানেজমেন্ট পেজে** প্রতিটা ক্যাটাগরির পাশে "📄 গাইড পেজ" বাটন — এখানে Pillar / Comparison / Design Ideas পেজ তৈরি/এডিট হবে।
3. নতুন পেজ তৈরি করলে (স্লাগ + টাইটেল দিয়ে) সাথে সাথেই একটা খালি ড্রাফট তৈরি হয়ে এডিটর খুলে যাবে — সেখানে "+ ব্লক যোগ করুন" দিয়ে একটার পর একটা ব্লক (Hero, Price Table, FAQ ইত্যাদি) যোগ করে ভরবেন।
4. প্রতিটা ব্লকে বাংলা+ইংরেজি দুই ভাষার ইনপুট পাশাপাশি থাকবে — দুটোই ভরতে হবে।
5. "ড্রাফট সেভ করুন" যতবার খুশি চাপা যাবে, পাবলিশ না করা পর্যন্ত পাবলিক সাইটে দেখাবে না। রেডি হলে "পাবলিশ করুন"।
6. `productRecommendation` ব্লকে যে প্রোডাক্টটা রেকমেন্ড করতে চান, তার আইডি বসাতে হবে — প্রোডাক্ট লিস্টে নামের নিচেই ছোট করে `#id` দেখানো আছে, সেখান থেকে কপি করবেন।

## যাচাই করা হয়েছে

- পুরো Mehediadmin প্রজেক্টে `npx tsc --noEmit` **শূন্য এরর**।
- `npx eslint` **কোনো ওয়ার্নিং/এরর ছাড়াই পাস**।

## এরপর কী বাকি

- বাকি ৪টা পেজের (Comparison, Installation Guide, App/Remote Guide, Design Ideas) আসল কনটেন্ট এই এডিটর দিয়ে ভরা (বা SQL সিড হিসেবে সরাসরি বসানো, Vangcur ডেলিভারির `sql/002_seed_pillar_example.sql`-এর মতো)।
- ড্র্যাগ-ড্রপ দিয়ে ব্লক reorder (এখন উপরে/নিচে বাটন দিয়ে reorder হয় — কাজ করে, কিন্তু ড্র্যাগ চাইলে পরে যোগ করা যাবে)।
- "Duplicate as template" বাটন UI-তে এখনো যোগ করা হয়নি (সার্ভার অ্যাকশন `duplicateGuidePage` রেডি আছে, শুধু UI বাটন বাকি)।
